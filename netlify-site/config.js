// Point this at your deployed backend API.
// Netlify hosts static files only — the API must live somewhere else
// (Render, Railway, Fly.io, etc). Update this one line after you deploy it.
window.API_BASE_URL = "https://trace-mvp.onrender.com";

// Local development against a backend running on your machine:
// window.API_BASE_URL = "http://localhost:4000";
